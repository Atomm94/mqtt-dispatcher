export enum ReceiveTopics {
    PING='PING',
    CRUD_MQTT = 'crud_mqtt',
    USER_LOG = 'userlog',
    MQTT_LOG = 'mqtt_log'

}

export enum SendTopics {
    MQTT_CRUD = 'mqtt_crud',
    MQTT_LOG = 'mqtt_log'
}
