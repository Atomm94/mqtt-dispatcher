export enum ReceiveTopics {
    PING='PING',
    CRUD_MQTT = 'crud_mqtt',
    LOG = 'log',
    MQTT_LOG = 'mqtt_log',
    MQTT_SOCKET = 'mqtt_socket'
}

export enum SendTopics {
    MQTT_CRUD = 'mqtt_crud',
    MQTT_LOG = 'mqtt_log'
}
